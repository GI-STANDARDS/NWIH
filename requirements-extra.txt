# ── Heavy / on-demand dependencies ──
# These are NOT installed by default. The Setup/Tools page
# in the dashboard manages these with CUDA/CPU choice.

sentence-transformers>=2.2.0
hdbscan>=0.8.0
umap-learn>=0.5.0
